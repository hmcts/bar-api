spring:
  cloud:
    propertiesvolume:
      paths: /mnt/secrets/bar
      aliases:
        bar.bar-S2S-SECRET: S2S_SECRET
        bar.api-POSTGRES-PASS: POSTGRES_PASSWORD
        bar.AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
